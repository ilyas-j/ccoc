# Configuration de la base de données
spring.datasource.url=jdbc:mysql://localhost:3306/coc_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=ilyasI12L
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuration JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Configuration JWT
app.jwtSecret=mySecretKey1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ
app.jwtExpirationInMs=604800000

# Configuration des logs
logging.level.com.stage.coc=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=ERROR

# Configuration du serveur
server.port=8080
server.servlet.context-path=/

# Configuration des fichiers uploadés
app.file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Configuration CORS
app.cors.allowed-origins=http://localhost:3000,https://your-frontend-domain.com

# Configuration de l'application
spring.application.name=coc-application

# Désactiver les bannières et logs non essentiels
spring.main.banner-mode=console
logging.level.root=WARN
logging.level.com.stage.coc=INFO

# Configuration de sécurité
spring.security.debug=false